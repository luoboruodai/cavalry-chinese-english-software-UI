# injector/
> L2 | 父级: ../CLAUDE.md

成员清单
cavalry_i18n_macos_quick_add_category.h / .cpp: Mac 完整分类源 getter 适配，复用已 pin 映像函数解析并验证双架构字节，截短显示不冒充完整源，写显示仍由共享 tabs 策略负责。
cavalry_i18n_quick_add_placeholder.h: Classic 空结果无缓存显示策略，exact owner/viewport 与当前英文 source 同时成立才调用一次平台 setter。
cavalry_i18n_macos_quick_add_placeholder.h / .cpp: 复用 Classic 锁定映像解析门，双架构函数字节与 Qt 类型大小证明 placeholder 只读边界，写入走 vendor setter。
cavalry_i18n_classic_rank.h: 共享 Classic 完整/前缀本地标题优先级补充（原厂 exact 1000、prefix 900 减归一化 UTF-8 标题字节数）；复用搜索 guard/query/provider，只在原厂显式 layout 排序前借用已验证 ListItem 评分并以持久索引归还，不新增排序、轮询、role 写入或 item 缓存，自动排序及未知 ABI 回退原厂行为。
cavalry_i18n_macos_classic_rank.h: macOS Classic 评分及同映像显示函数解析接口；向共享策略提供已验证的 getter/setter、priority 模式与 exact item 类型判断，不泄露原厂私有字段布局。
cavalry_i18n_macos_classic_rank.cpp: macOS 已加载 Cavalry 2.7.2 双架构评分 ABI 校验与导出解析；显示适配器复用同一 pin 映像与函数字节验证，以映像 UUID、函数地址和 ElementListItem primary vtable 限定调用，检查失败返回空 API，排序回调不读文件。
cavalry_i18n_quick_add_tabs.h: Quick Add 顶部类别标签显示边界；平台以已验证 ABI 提供完整源文本，共享策略只更新限定 owner 下的 QLabel 显示，保留 vendor 分类身份。
cavalry_i18n_quick_add_display.h: 跨平台 FastQuickAdd 显示适配器；各平台独立验证 ExtensionLayer 映像与 exact FastQuickAdd source-model 链，Windows 另锁定完整绘制/复制析构函数摘要及唯一元类型来源，paint/sizeHint 仅使用 Qt registered copy 的标题/类别标签投影，标签先遵循原厂 Atomic→Utility、Beta→Experimental 显示规则再复用译文，源分类与命令不变，未知 model/payload 保持原始 index 与值透传；原 delegate 销毁回退 Qt 英文 delegate，vendor replacement 清理旧 wrapper，model 变更在首个绘制前同步。

CavalryTranslatorInjector.mm: Classic 空结果仅在 exact 列表/viewport 绘制前把原厂英文投影一次，当前译文阻断重复 setter； Add Layer 两种 owner 的搜索输入保持原文，FastQuickAdd 在非 Paint 的挂接路径安装双语过滤索引，并在已验证 vendor 的 delegate 绘制副本翻译标题，Classic 使用名称/说明索引与标题适配器保留英文检索依据，并在已验证平台 ABI 下复用共享完整及前缀本地标题评分； macOS Objective-C++ runtime 翻译注入器；以 `(context, source)` 哈希复用生成表，在 Qt runtime minor 确认后把五条 `MenuBarManager` exact action 交给独立 TransformTool ABI 适配器，translator 安装后先启用轻量 ContextMenu owner 路径、启动回补后再开放完整 dirty 事件翻译；对 `assets::Window` 创建的 parentless QMenu/QAction，只在单事件循环内把嵌套 ContextMenu 规范到根菜单并以弱引用承接 owner，使 `Replace...` 与动态 `Create Composition based on %1` 共用同一可信 producer，同时保持既有菜单首帧、模型 identity、MessageBar 与 capture-only inventory 边界。
cavalry_i18n_macos_tool_help_text_path.h: macOS TransformTool text-path 窄接口；只接受启动期五条 exact action 译文并返回带逐 source 成功/回退计数的原子诊断快照，另以版本化只读 C ABI 供同进程证据驱动计算动作前后增量，使 Objective-C++ 翻译表所有权与 vendor C++ ABI 防火墙保持单向依赖。
cavalry_i18n_macos_tool_help_text_path.cpp: macOS Cavalry 2.7.2 自绘 Path 适配器；从已加载 Mach-O `__LINKEDIT` 恢复真实 Skia forward target，以 ExtensionLayer/Core/skia 双 slice UUID、调用机器码和 `Core GetPath → getOrCreateTextPath → setupToolHelp action` 三层 return address 收敛进程级 interpose，仅为五条 action 换用已验证系统 CJK font，并原子记录逐 source 成功/回退次数以区分启动累计值与当前动作增量；快捷键 prefix、未知 caller、字体/ABI 漂移及空 Path 全部转发英文。
cavalry_i18n_translation_policy.h: 翻译查询边界；跨平台把 CogTool 动态 Pitch、Color Settings/Mesh Explorer 模板、单索引占位文字、8 条 ordinary-Qt 残留及 MenuBarManager 的具体 Add Layer 快捷键变体限制在精确 context；另将 Tag `Assign Tag to Selection: ` 与 Assets `Create Composition based on %1` 声明为双平台 owner-scoped 邻接 key，只允许 macOS/Windows 各自已采证 producer 消费并永久排除 source fallback。
cavalry_i18n_input_policy.h: Qt 选择输入值的双平台共享保护边界；按 editable/QFontComboBox 与父链覆盖编辑器和弹出列表，不以词典命中或阻断信号冒充显示/业务分离；CompleterLineEdit 的值在 parentless/重挂接期间也始终按输入保护，父链只用于索引挂接，不决定是否可翻译输入。
cavalry_i18n_quick_add_context.h: Quick Add 窄 UI 上下文边界；集中维护 `cavalry::FastQuickAddWindow`/`QuickAddWindow` exact owner、`SearchBar`/`Widget` 父链、`CompleterLineEdit` 输入保护与唯一 `QListView` 搜索框发现，供 Fast 与 Classic 搜索策略复用，不读取 model 数据或改写用户输入。
cavalry_i18n_search_policy.h: Add Layer FastQuickAdd 共享搜索边界；消费窄 context 的 exact owner/search-box guard，别名代理仅在 Fast owner 下接受 `QListView`/vendor `QSortFilterProxyModel` 的 role 257 与 `FastQuickAddModel`，把当前语言别名与 Unicode code-point subsequence 过滤代理置于 vendor filter 内侧，保留 vendor role 0/257 模糊匹配、view/index/role 256/排序/写回/flags/动态行/源生命周期并防止重复包裹；view 整体替换 vendor filter 时恢复旧 filter 的原始 source、在当前 filter 幂等重挂接，未知 model fail-closed；查询优先由原始搜索框驱动，未绑定时只读取 vendor filter 的 public `filterRegularExpression`，明确不套 dock 模型，重复 provider 输出不发脏信号；view 析构断开输入/源变化连接并恢复仍存活的 vendor source，清理自有代理，反向 filter 先销毁由 QPointer 防止重复释放。 共享 Fast 搜索合同与 Windows 产品统一使用 QT_NO_KEYWORDS，防止本地默认 Qt 宏掩盖编译兼容性问题。
cavalry_i18n_search_descriptions.h: Add Layer Classic 描述搜索只读入口；按当前语言完整 QLabel 说明反向查找英文 nodeInfo/layerInfo，QHash 按语言懒构建并保留碰撞的全部英文值，未知输入 fail-closed，不进入通用翻译 fallback 或显示/模型 identity。
cavalry_i18n_classic_search.h: Classic QuickAddWindow/ListWidget 专用 role0 投影；以每个 QListWidget 视图持有的 QPersistentModelIndex/源英文缓存幂等重建 source，复用同一 exact owner 下的搜索框并按 vendor cleanup；标题别名与公开 itemWidget QLabel 的英文说明均只作 side data，只有别名或说明独立命中当前清理 query 时才追加该 query token，U+FFFE 仅分隔 source/token；原生排序前恢复 source、投影时暂停自动排序，排序方向与比较器仍由 Qt/vendor 持有；临时投影阻断 QListWidget itemChanged 而保留 model 信号，避免 vendor 同步编辑回调重入，itemWidget 唯一标题 QLabel 单独投影为本地显示，覆盖动态插入/删除/排序/reset 与 generic DisplayRole 回写而不改说明标签、command/flags 或未知 UserRole。
generated_translations.inc: 由 `tools/generate_embedded_translations.js` 从 `tools/*.ts` 与 display-only 模型名词典自动生成、并携带生成式 GEB L3 契约的 C++ 编译期翻译表，不可手动编辑。
generated_quick_add_descriptions.inc: 由 `tools/generate_quick_add_descriptions.js` 从四语 JSON 的 nodeInfo/layerInfo 稳定身份生成，提供英文与三语完整说明的编译期字节表；不可手工编辑，不进入通用 Qt 翻译表。
libCavalryTranslatorInjector.dylib: 不纳入 Git 的 universal macOS 平台构建产物；由 `tools/build_translator_injector.sh` 从当前源码生成并以 `@loader_path` 优先解析所选 app 同目录 Qt/libskia，Tauri 打包时作为 bundle resource 嵌入。
windows/: Classic 空结果仅在已验证 vendor ABI 与 exact ListWidget/真实 viewport 内投影既有三语提示，重复 Paint 不重复写入；Windows Qt 6.6.3 x64 MSVC 双模块；`qpa/qwindows.dll` 以版本化私有 QPA ABI 委托持久化原厂平台插件，只有安装根 exact manifest v1、三项实际 SHA-256 与非英语 marker 同时通过才显式传语言启动 `generic/cavalryi18n.dll`，不写进程环境，翻译失败保留原厂 integration。generic 显示层与 macOS 对齐逐行翻译 `标题\n说明` tooltip，并以显示属性 + 厂商父系/对话框双门投影 selected-count/认证倒计时、Mesh Explorer 整数 QLabel、Color Settings QComboBox 模板与单索引 QPlainTextEdit 占位文字；ExtensionLayer 聚合 helper、placeholder、MessageBar 与 text-path 四条精确 IAT 边界，后者覆盖三十六项静态文本（含 SkeletonTool Bone Tool 四组提示）及 CogTool 动态整数 Pitch，单独的 `Space` 等纯键位保持原文。MessageBar 只批准 history/live 双 return 与单条 HTML 尾部正文并排除 `js_logger`。插件永久 PIN 是任一 aggregate IAT 安装写入的前置资格，text-path 另在私有 Core/skia 映像逐范围通过 PE64 timestamp/size、精确导出 RVA 与关键 ownership bytes 后独立 PIN 并允许 CJK Path；每次 callback 继续复核完整 caller 字节包络及精确 context。callback 使用无静态析构的 process-lifetime 槽，restore 失败保留 forward-only original；诊断以 64 位 source mask/原子计数进入显式 marker，保留 Pitch bit 28 并分配 Bone bits 29–36，渲染路径无 IO。

依赖边界:

macOS 注入器在 Cavalry 进程内通过 DYLD_INSERT_LIBRARIES 加载；Windows 从所有原生入口必经的 QPA 代理委托原厂窗口系统，再显式启动 generic 翻译 runtime，环境自动发现仅保留为兼容合同。二者均依赖 Cavalry 的 Qt 6.6.3 runtime ABI，不携带第二套 Qt；Windows QPA 额外锁定同版本私有头，macOS 额外依赖 AppKit，并只在 Cavalry 2.7.2 的 ExtensionLayer/Core/skia 双架构合同完整一致时开放五条 TransformTool CJK Path。`generated_translations.inc` 是 `tools/*.ts` 与 display-only 模型词典的共享机器投影，任何翻译变更必须通过 `generate_embedded_translations.js` 重生成，两个平台构建入口都必须在编译前重建且不得手改或分叉该表；dylib/DLL 仅由对应平台 Runner 生成并由最终 DMG/NSIS 承担发布事实。

法则: 翻译表自动生成·精确哈希保持 first-match-wins·模型 niceName 保留英文·显示层模型名可翻译·已知基名数字后缀仅在基名有嵌入译文时投影·selected-count/认证倒计时保持严格 QLabel；Mesh Explorer、Color Settings 与单索引动态模板还必须命中已采证父系/对话框来源·8 条跨平台 ordinary-Qt 残留、具体快捷键变体及两条双平台 owner-scoped 邻接 key 禁止进入 source fallback，只允许 exact context 或平台内已采证 owner/控件结构回补；邻接 key 不计入共享 8 条，任何新平台必须先取得真实 producer 证据·parentless context menu 只承接同一事件循环内的 exact producer，嵌套菜单先规范到根 QMenu，过期即 fail-closed·动态 QComboBox 仅改 DisplayRole 且 UserRole/currentIndex 不变·单索引 QPlainTextEdit 仅改精确 placeholderText 且绝不读写正文·无关同文控件保持原文·Scene View 图层列表可翻译·Time Editor 自身 item view 与 model role 保留英文·可编辑/字体 Combo 的选项、编辑值和弹出列表保持原值；其他 QLineEdit 既有显示翻译路径由独立搜索工作项处理·QTextEdit append 只翻译日志正文与有限日志模板·aboutToShow 菜单同步翻译·交互事件禁止全局刷新·普通运行禁止 inventory 写盘·动态菜单规则化·原生库由对应平台生成且不纳入 Git·只加载目标 app 的单套 Qt·禁止引入目标 Qt 缺失符号·Qt ABI 锁定·ExtensionLayer 仅可用共享合同内已验证的 helper/placeholder/MessageBar/text-path 边界；macOS 唯一批准的进程级 Skia interpose 必须先从 Mach-O 恢复原 target，再同时命中五条 exact action、三层 caller、双 slice UUID/符号地址/代码包络与 CJK glyph，任何失败立即 forward 英文，prefix 永不进入 action 表；Windows 禁止 vendor `.text` / Skia / libc / QPainter 全局 hook，继续使用 owner-scoped IAT·MessageBar 必须双 caller/末尾 `<br>`/exact body 同时命中且排除 `js_logger`·context-only 专用词条禁止进入 source fallback，unknown/纯快捷键 prefix 原样透传·CogTool Pitch 只允许 canonical 32-bit `int` 后缀·CJK 自绘任一失败回退英文·IAT 卸载必须 owner-only 且 restore 失败 fail-open·QString 赋值槽只能由 canonical setter 尾跳解码·禁止 `__cstring` 补丁

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
